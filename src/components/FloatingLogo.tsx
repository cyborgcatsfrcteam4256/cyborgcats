import { useState, useEffect } from 'react';
import cyborgCatsLogo from '@/assets/cyborg-cats-logo.png';

export const FloatingLogo = () => {
  return null; // Removed floating cats - will use static placements instead
};